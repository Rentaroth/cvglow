const { responseSkillsSchema } = require('./response_skills_swagger.schema');
const { requestSkillsSchema } = require ('./request_skills_swagger.schema');

module.exports = {
  responseSkillsSchema,
  requestSkillsSchema,
}