const { requestLanguagesSchema } = require('./request_languages_swagger.schema');
const { responseLanguageSchema } = require('./responseLanguage_swagger.schema');

module.exports = {
  requestLanguagesSchema,
  responseLanguageSchema,
}