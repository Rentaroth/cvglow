const { requestExperienceSchema } = require('./request_experience_swagger.schema');
const { responseExperienceSchema } = require('./response_experience_swagger.schema');

module.exports = {
  requestExperienceSchema,
  responseExperienceSchema,
}