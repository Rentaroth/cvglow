const { personalInfoSchema } = require('./personalInfo_swagger.schema');
const { requestPersonalInfoSchema } = require('./request_personal_info_swagger.schema');

module.exports = {
  personalInfoSchema,
  requestPersonalInfoSchema,
}