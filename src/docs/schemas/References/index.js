const { referencesSchema } = require('./response_references_swagger.schema');
const { requestReferencesSchema } = require('./request_references_swagger.schema');

module.exports = {
  referencesSchema,
  requestReferencesSchema,
}